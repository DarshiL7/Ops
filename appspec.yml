version: 0.0
os: linux
files:
  - source: /appspec.yml
    destination: /home/php7encode/projects/devops
hooks:
  BeforeInstall:
      - location: backup.sh
        timeout: 300
        runas: root 

  AfterInstall:
      - location: gitStatus.sh
        timeout: 300
        runas: root 
        
  ApplicationStart:
    - location: encrypt.sh
      timeout: 300
      runas: root

  ValidateService:
    - location: gulp.sh
      timeout: 300
      runas: root
